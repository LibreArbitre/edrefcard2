version: '3.8'

services:
  edrefcard:
    build: .
    volumes:
      - edrefcard_data:/app/www/configs
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped

volumes:
  edrefcard_data:
